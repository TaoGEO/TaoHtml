This benchmark run explicitly binds `input/prompt.md` as the current task instruction and `input/materials/legacy-deck.html` as the source material for this report. Please use $taohtml to upgrade the bound HTML into a single-screen live presentation. Keep every visible page word, viewpoint, and page order unchanged; upgrade only the visual system and TaoHtml Runtime. Put the complete runnable deliverable in `deliverable/`, with `deliverable/index.html` as the entry point.
